@model string
@*@model List<Graph.Models.ZackRank>*@
@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script type="text/javascript">
    google.charts.load('current', {packages: ['corechart']});
    google.charts.setOnLoadCallback(drawChart);

    function drawChart() {
        // Define the chart to be drawn.
        var data = new google.visualization.DataTable();
        data.addColumn('string', 'Date');
        data.addColumn('number', 'Rank');
        data.addRows(@Html.Raw(Model));
        //data.addRows([
        //    ['2018-01-01', 1],
        //    ['2018-01-02', 1],
        //    ['2018-01-03', 2]
        //]);

        // Instantiate and draw the chart.
        var chart = new google.visualization.LineChart(document.getElementById('myPieChart'));
        //alert(chart);
        chart.draw(data, null);
    }
</script>
<h1>Details</h1>

<div>
    @*@{
            foreach (var zr in Model)
            {
                var output = zr.Symbol + "<br/>";
                @output
            }
        }*@

    <div id="myPieChart"></div>
    @*<input type="button" onclick="javascript:drawChart();" value="Draw Chart!!"/>*@

    @*<h4>ZackRank</h4>
        <hr />
        <dl class="row">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Symbol)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Symbol)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Rank)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Rank)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.RankDate)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.RankDate)
            </dd>
        </dl>*@
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
