@model string
@*@model List<DM.ZackRank>*@
@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";

}
<script type="text/javascript">
    google.charts.load('current', {packages: ['corechart']});
    google.charts.setOnLoadCallback(drawChart);

    function drawChart() {
        // Define the chart to be drawn.
        var data = new google.visualization.DataTable();
        data.addColumn('string', 'Date');
        data.addColumn('number', 'Rank');
        data.addColumn('number', 'Momentum');
        data.addRows(@Html.Raw(Model));
        
        // Instantiate and draw the chart.
        var chart = new google.visualization.LineChart(document.getElementById('Details'));

        //alert(chart);
        chart.draw(data, null);
    }
</script>
<h1>Details</h1>

<div>
    @*@{
            foreach (var zr in Model)
            {
                var output = zr.Symbol + "<br/>";
                @output
            }
        }*@

    <div id="Details"></div>
    
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
